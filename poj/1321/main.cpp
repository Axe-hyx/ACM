#include <cstdio>
int n,m;
int main(){
    while(scanf("%d%d", &n,&m)==2){
        if(n==-1 || m == -1) break;
        for(int i=0;i<n;++i){
            for(int k=0;k<n;++k){

            }
        }
    }
    return 1;
}
